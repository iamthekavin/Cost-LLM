# Cost-LLM Shared Package

This package contains shared Pydantic v2 schemas and JSON contracts consumed across all services:
- `router-core`
- `model-gateway`
- `control-plane`
- `benchmark`

## Installation
For local development:
```bash
pip install -e shared
```
